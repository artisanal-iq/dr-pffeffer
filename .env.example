# -------------------------------------------
# Core application settings
# Copy this file to .env.local and fill in the values before running pnpm dev.
# -------------------------------------------

# Public site URL used for redirects and emails.
# Local development should typically remain http://localhost:3000.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Supabase project connection details (required).
# The URL should match https://YOUR-PROJECT.supabase.co.
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"

# Supabase public anon key. Retrieve from Supabase → Project Settings → API.
NEXT_PUBLIC_SUPABASE_ANON_KEY="replace-with-anon-key"

# Service role key used for server-side scripts and migrations.
# Optional for local UI development, required for production deployments and database migrations.
SUPABASE_SERVICE_ROLE_KEY="replace-with-service-role-key"

# Postgres connection string used by Drizzle migrations and background jobs.
# Example: postgres://postgres:password@db.<hash>.supabase.co:5432/postgres
SUPABASE_DB_URL="postgres://USER:PASSWORD@HOST:5432/postgres"

# OpenAI API key for AI-powered journal summaries. Leave empty to disable AI features locally.
OPENAI_API_KEY=""

# Logging endpoints
# Point these to your logging provider (Axiom, Logflare, Datadog, etc.).
# When unset, logging middleware should no-op.
LOG_INGEST_URL="https://logs.example.com/api/v1/ingest"
LOG_INGEST_TOKEN="replace-with-log-token"
LOG_ENVIRONMENT="local"
